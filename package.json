{
  "name": "etf-tracker",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && python main.py"
  },
  "engines": {
    "node": ">=20"
  }
}
